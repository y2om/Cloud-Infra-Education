name: CI - Validate Manifests & Trivy Security Scan

on:
  push:
    branches:
      - main
      - "feat/#*"
  pull_request:
    branches:
      - main
      - "feat/#*"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-and-scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ================= [1] YAML 파일 문법 검증 =================
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install PyYAML
        run: pip install pyyaml

      - name: Validate YAML syntax
        run: |
          echo "🔍 Validating YAML files..."
          find . -name "*.yaml" -o -name "*.yml" | while read file; do
            echo "Checking: $file"
            python3 -c "import yaml; list(yaml.safe_load_all(open('$file')))" || exit 1
          done
          echo "✅ All YAML files are valid!"

      # ================= [2] Kubernetes YAML 검증 (kubectl) =================
      - name: Setup kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: Validate Kubernetes manifests
        run: |
          echo "🔍 Validating Kubernetes manifests..."
          find base -name "*.yaml" -o -name "*.yml" | while read file; do
            echo "Validating: $file"
            # kubectl dry-run이 실패해도 계속 진행 (일부 파일은 Kubernetes 매니페스트가 아닐 수 있음)
            kubectl apply --dry-run=client -f "$file" 2>&1 || echo "⚠️ Skipping non-Kubernetes file: $file"
          done
          echo "✅ Kubernetes manifests validation completed!"

      # ================= [3] Trivy 보안 스캔 단계 =================
      # Kubernetes 매니페스트 파일의 보안 취약점을 검사합니다.
      - name: Run Trivy vulnerability scanner (Filesystem)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'          # 파일 시스템 전체를 스캔
          scan-ref: '.'            # 현재 경로 기준
          format: 'table'          # 결과를 테이블 형태로 출력
          severity: 'CRITICAL,HIGH' # 치명적(Critical), 높음(High) 취약점 감지
          exit-code: '0'           # 취약점 발견해도 경고만 표시 (배포 계속 진행)
        continue-on-error: true    # 오류가 있어도 다음 단계 계속 진행

      # ================= [4] Trivy Config 스캔 (Kubernetes 설정 파일) =================
      - name: Run Trivy config scanner (Kubernetes)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'config'      # 설정 파일 스캔
          scan-ref: 'base/'        # base 디렉토리의 Kubernetes 매니페스트
          format: 'table'
          severity: 'CRITICAL,HIGH'
          exit-code: '0'           # 취약점 발견해도 경고만 표시 (배포 계속 진행)
        continue-on-error: true    # 오류가 있어도 다음 단계 계속 진행

      # ================= [참고] ArgoCD 자동 배포 =================
      # ArgoCD가 Git 저장소를 모니터링하고 있어서,
      # 이 워크플로우가 성공하면 자동으로 ArgoCD가 변경사항을 감지하여 배포합니다.
      - name: Workflow completed
        run: |
          echo "✅ All validations passed!"
          echo "📦 ArgoCD will automatically detect and deploy the changes."
          echo "🔗 Check ArgoCD Application status after push."
